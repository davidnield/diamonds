---
title: "Github Actions Example"
format: html
editor: visual
---

```{r}

library(ggplot2)

current_datetime <- timestamp()

diamonds |> 
ggplot(aes(x = carat, y = price, color = cut)) +
  geom_point() +
  geom_smooth() +
  labs(title = current_datetime)

print('foobar')
  
```
